services:
  pi-web-ui:
    image: pi-web-ui:0.73.0
    container_name: pi-web-ui
    restart: unless-stopped
    ports:
      - "5173:5173"
    volumes:
      - ./data:/app/packages/web-ui/example/data
      - ./pi-storage.config.json:/app/packages/web-ui/example/pi-storage.config.json:ro

